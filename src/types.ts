export interface AppLink {
  id: string;
  title: string;
  url: string;
  iconName: string;
  category: string;
}

export interface SliderItem {
  id: string;
  image: string;
  title: string;
  subtitle: string;
}
